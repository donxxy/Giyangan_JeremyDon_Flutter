name: cpu_membership_card
description: CPU student organization digital membership card (Chapter 4 widget tree exercise).
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cached_network_image: ^3.4.1
  url_launcher: ^6.3.1
  # google_fonts removed due to web compatibility issues; using default TextStyle

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true